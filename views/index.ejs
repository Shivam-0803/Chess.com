
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Chess Game with Video Chat</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">

    <style>
      /* Chessboard and Game UI */
      .chessboard {
        display: grid;
        grid-template-columns: repeat(8, 1fr);
        grid-template-rows: repeat(8, 1fr);
        width: 100%;
        max-width: min(70vh, 500px);
        aspect-ratio: 1/1;
        border: 3px solid #8B4513;
        box-shadow: 
          0 0 0 1px #654321,
          0 4px 12px rgba(0, 0, 0, 0.4);
        background: #fff;
        margin: 0 auto;
        border-radius: 4px;
        overflow: hidden;
      }

      .square {
        display: flex;
        align-items: center;
        justify-content: center;
        position: relative;
        user-select: none;
        transition: all 0.2s ease;
        width: 100%;
        height: 100%;
        box-sizing: border-box;
      }

      .square.light {
        background: #F0D9B5;
      }

      .square.dark {
        background: #B58863;
      }

      .square:hover {
        filter: brightness(1.1);
      }

      .piece {
        font-size: clamp(32px, 6vw, 52px);
        font-weight: 600;
        cursor: grab;
        transition: all 0.2s ease;
        font-family: "Segoe UI Symbol", "Noto Sans Symbols", "DejaVu Sans", "Arial Unicode MS", sans-serif;
        text-align: center;
        line-height: 1;
        position: relative;
        z-index: 2;
      }

      .piece.white {
        color: #ffffff;
        text-shadow: 
          1px 1px 0 #000000,
          -1px -1px 0 #000000,
          1px -1px 0 #000000,
          -1px 1px 0 #000000,
          0 2px 4px rgba(0,0,0,0.6);
      }

      .piece.black {
        color: #2d3748;
        text-shadow: 
          1px 1px 0 #ffffff,
          -1px -1px 0 #ffffff,
          1px -1px 0 #ffffff,
          -1px 1px 0 #ffffff,
          0 1px 2px rgba(255,255,255,0.4);
      }

      .dragging {
        opacity: 0.3;
        transform: scale(1.1);
      }

      .dot {
        position: absolute;
        width: 25%;
        height: 25%;
        background-color: rgba(72, 187, 120, 0.7);
        border-radius: 50%;
        pointer-events: none;
        z-index: 3;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
      }

      .dot.capture {
        width: 85%;
        height: 85%;
        background-color: transparent;
        border: 3px solid rgba(245, 101, 101, 0.8);
        border-radius: 50%;
      }

      .square.selected {
        box-shadow: inset 0 0 0 3px #4299e1;
        z-index: 1;
      }

      .square.highlight {
        background-color: rgba(72, 187, 120, 0.4) !important;
      }

      .square.last-move {
        box-shadow: inset 0 0 0 2px #f6d55c;
      }

      .square::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        pointer-events: none;
      }

      .flipped {
        transform: rotate(180deg);
      }

      .flipped .piece {
        transform: rotate(180deg);
      }

      /* Video Styles */
      .video-container {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        border-radius: 16px;
        padding: 3px;
      }

      .video-inner {
        background: #1a202c;
        border-radius: 13px;
        overflow: hidden;
      }

      video {
        border-radius: 13px;
      }

      /* Chat Styles */
      .chat-container {
        background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
        border-radius: 16px;
        padding: 3px;
      }

      .chat-inner {
        background: #1a202c;
        border-radius: 13px;
        height: 100%;
        display: flex;
        flex-direction: column;
      }

      .chat-messages {
        flex: 1;
        overflow-y: auto;
        padding: 16px;
        scrollbar-width: thin;
        scrollbar-color: #4a5568 #2d3748;
      }

      .chat-messages::-webkit-scrollbar {
        width: 6px;
      }

      .chat-messages::-webkit-scrollbar-track {
        background: #2d3748;
        border-radius: 3px;
      }

      .chat-messages::-webkit-scrollbar-thumb {
        background: #4a5568;
        border-radius: 3px;
      }

      .message {
        margin-bottom: 12px;
        padding: 8px 12px;
        border-radius: 12px;
        max-width: 80%;
        word-wrap: break-word;
      }

      .message.own {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        color: white;
        margin-left: auto;
        text-align: right;
      }

      .message.other {
        background: #2d3748;
        color: #e2e8f0;
      }

      .message-sender {
        font-size: 0.75rem;
        opacity: 0.7;
        margin-bottom: 2px;
      }

      /* Button Styles */
      .btn-primary {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        transition: all 0.3s ease;
      }

      .btn-primary:hover {
        transform: translateY(-2px);
        box-shadow: 0 10px 20px rgba(102, 126, 234, 0.3);
      }

      .btn-success {
        background: linear-gradient(135deg, #48bb78 0%, #38a169 100%);
      }

      .btn-danger {
        background: linear-gradient(135deg, #f56565 0%, #e53e3e 100%);
      }

      /* Timer Styles */
      .timer-container {
        background: linear-gradient(135deg, #2d3748 0%, #4a5568 100%);
        border-radius: 16px;
        padding: 20px;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
      }

      .timer {
        font-family: 'Courier New', monospace;
        font-size: 1.5rem;
        font-weight: bold;
        text-align: center;
        padding: 12px 20px;
        border-radius: 12px;
        background: #1a202c;
        color: #e2e8f0;
        box-shadow: inset 0 2px 4px rgba(0, 0, 0, 0.3);
      }

      /* Connection Status */
      .status-indicator {
        display: inline-block;
        width: 12px;
        height: 12px;
        border-radius: 50%;
        margin-right: 8px;
      }

      .status-connected {
        background: #48bb78;
        animation: pulse 2s infinite;
      }

      .status-connecting {
        background: #ed8936;
        animation: pulse 1s infinite;
      }

      .status-disconnected {
        background: #f56565;
      }

      @keyframes pulse {
        0% { opacity: 1; }
        50% { opacity: 0.5; }
        100% { opacity: 1; }
      }

      /* Custom animations */
      @keyframes pulse {
        0% { opacity: 1; }
        50% { opacity: 0.5; }
        100% { opacity: 1; }
      }

      .animation-delay-2000 {
        animation-delay: 2s;
      }

      .animation-delay-4000 {
        animation-delay: 4s;
      }

      .animation-delay-6000 {
        animation-delay: 6s;
      }

      /* Enhanced chat styles */
      .message.own {
        background: linear-gradient(135deg, #3b82f6 0%, #8b5cf6 100%);
        color: white;
        margin-left: auto;
        text-align: right;
        border: 1px solid rgba(255, 255, 255, 0.2);
      }

      .message.other {
        background: rgba(255, 255, 255, 0.1);
        backdrop-filter: blur(10px);
        color: #e2e8f0;
        border: 1px solid rgba(255, 255, 255, 0.2);
      }

      /* Scrollbar styles */
      .scrollbar-thin::-webkit-scrollbar {
        width: 6px;
      }

      .scrollbar-thin::-webkit-scrollbar-track {
        background: rgba(255, 255, 255, 0.1);
        border-radius: 3px;
      }

      .scrollbar-thin::-webkit-scrollbar-thumb {
        background: rgba(255, 255, 255, 0.3);
        border-radius: 3px;
      }

      .scrollbar-thin::-webkit-scrollbar-thumb:hover {
        background: rgba(255, 255, 255, 0.5);
      }

      /* Responsive */
      @media (max-width: 1024px) {
        .chessboard {
          max-width: min(65vh, 450px);
        }
      }

      @media (max-width: 768px) {
        .chessboard {
          max-width: min(90vw, 420px);
          border-width: 2px;
        }
        
        .timer {
          font-size: 1.1rem;
          padding: 10px 16px;
        }
        
        .piece {
          font-size: clamp(36px, 7vw, 54px);
        }
      }

      @media (max-width: 640px) {
        .chessboard {
          max-width: min(95vw, 400px);
          border-width: 2px;
        }
        
        .timer {
          font-size: 1rem;
          padding: 8px 12px;
        }
        
        .piece {
          font-size: clamp(34px, 6.5vw, 48px);
        }
      }

      @media (max-width: 480px) {
        .chessboard {
          max-width: min(98vw, 380px);
        }
        
        .piece {
          font-size: clamp(32px, 6vw, 44px);
        }
      }
    </style>
  </head>

  <body class="bg-gradient-to-br from-gray-900 via-black to-gray-800 text-white min-h-screen relative overflow-x-hidden">
    <!-- Dark Animated Background Elements -->
    <div class="fixed inset-0 z-0">
      <div class="absolute top-20 left-10 w-48 h-48 md:w-72 md:h-72 bg-gray-700 rounded-full mix-blend-multiply filter blur-xl opacity-10 animate-pulse"></div>
      <div class="absolute top-40 right-10 w-48 h-48 md:w-72 md:h-72 bg-gray-600 rounded-full mix-blend-multiply filter blur-xl opacity-10 animate-pulse animation-delay-2000"></div>
      <div class="absolute bottom-20 left-20 w-48 h-48 md:w-72 md:h-72 bg-gray-800 rounded-full mix-blend-multiply filter blur-xl opacity-10 animate-pulse animation-delay-4000"></div>
      <div class="absolute bottom-40 right-20 w-48 h-48 md:w-72 md:h-72 bg-gray-500 rounded-full mix-blend-multiply filter blur-xl opacity-10 animate-pulse animation-delay-6000"></div>
    </div>
    
    <!-- Dark overlay -->
    <div class="fixed inset-0 z-10 bg-black/30 backdrop-blur-sm"></div>
    <!-- Header -->
    <header class="relative z-20 bg-black/20 backdrop-blur-lg border-b border-gray-700/50 shadow-lg">
      <div class="container mx-auto px-4 py-3 md:py-4">
        <div class="flex items-center justify-between flex-wrap gap-2">
          <h1 class="text-xl md:text-3xl font-bold bg-gradient-to-r from-yellow-400 via-orange-400 to-red-400 bg-clip-text text-transparent drop-shadow-lg">
            <i class="fas fa-chess-king mr-2 text-yellow-400"></i>
            Chess Master Pro
          </h1>
          <div class="flex items-center space-x-2 md:space-x-4">
            <div id="connectionStatus" class="flex items-center text-xs md:text-sm bg-black/20 px-2 md:px-3 py-1 md:py-2 rounded-full backdrop-blur-md border border-gray-600/50">
              <span class="status-indicator status-disconnected"></span>
              <span class="hidden sm:inline">Waiting for connection...</span>
              <span class="sm:hidden">•••</span>
            </div>
            <div id="playerRole" class="px-2 md:px-4 py-1 md:py-2 bg-gray-800/50 backdrop-blur-md rounded-full text-xs md:text-sm border border-gray-600/50">
              <i class="fas fa-user mr-1"></i>
              <span>Connecting...</span>
            </div>
          </div>
        </div>
      </div>
    </header>

    <main class="relative z-20 container mx-auto px-2 md:px-4 py-3 md:py-6">
      <div class="grid grid-cols-1 lg:grid-cols-4 gap-4 md:gap-8 h-full">
        
        <!-- Video Chat Column -->
        <div class="lg:col-span-1 space-y-6">
          <!-- Opponent Video -->
          <div class="bg-black/30 backdrop-blur-lg rounded-2xl md:rounded-3xl p-1 border border-gray-600/30 shadow-2xl">
            <div class="bg-black/60 backdrop-blur-md rounded-[14px] md:rounded-[22px] overflow-hidden">
              <div class="relative aspect-video">
                <video id="remoteVideo" autoplay playsinline class="w-full h-full object-cover"></video>
                <div class="absolute inset-0 flex items-center justify-center text-gray-300 video-fallback bg-gradient-to-br from-blue-500/20 to-purple-500/20">
                  <div class="text-center">
                    <i class="fas fa-user-circle text-5xl mb-3 text-blue-400"></i>
                    <p class="text-sm font-medium">Waiting for opponent...</p>
                  </div>
                </div>
                <div class="absolute bottom-3 left-3 bg-black/60 backdrop-blur-sm px-3 py-1 rounded-full text-xs border border-white/20">
                  <i class="fas fa-user mr-1 text-blue-400"></i>Opponent
                </div>
              </div>
            </div>
          </div>

          <!-- Your Video -->
          <div class="bg-black/30 backdrop-blur-lg rounded-2xl md:rounded-3xl p-1 border border-gray-600/30 shadow-2xl">
            <div class="bg-black/60 backdrop-blur-md rounded-[14px] md:rounded-[22px] overflow-hidden">
              <div class="relative aspect-video">
                <video id="localVideo" autoplay playsinline muted class="w-full h-full object-cover"></video>
                <div class="absolute inset-0 flex items-center justify-center text-gray-300 video-fallback bg-gradient-to-br from-green-500/20 to-blue-500/20">
                  <div class="text-center">
                    <i class="fas fa-video-slash text-5xl mb-3 text-green-400"></i>
                    <p class="text-sm font-medium">Click "Start Video" below</p>
                  </div>
                </div>
                <div class="absolute bottom-3 left-3 bg-black/60 backdrop-blur-sm px-3 py-1 rounded-full text-xs border border-white/20">
                  <i class="fas fa-user mr-1 text-green-400"></i>You
                </div>
              </div>
            </div>
          </div>

          <!-- Video Controls -->
          <div class="bg-black/30 backdrop-blur-lg rounded-2xl md:rounded-3xl p-4 md:p-6 border border-gray-600/30 shadow-2xl space-y-3 md:space-y-4">
            <button id="startVideo" class="w-full bg-gradient-to-r from-gray-600 to-gray-800 hover:from-gray-700 hover:to-gray-900 text-white py-3 md:py-4 rounded-xl md:rounded-2xl font-semibold transition-all duration-300 transform hover:scale-105 shadow-lg text-sm md:text-base">
              <i class="fas fa-video mr-2"></i>Start Video Call
            </button>
            <div class="grid grid-cols-2 gap-2 md:gap-3">
              <button id="toggleMute" class="bg-gradient-to-r from-gray-700 to-gray-900 hover:from-gray-800 hover:to-black text-white py-2 md:py-3 rounded-lg md:rounded-xl font-medium transition-all duration-300 transform hover:scale-105 shadow-lg text-xs md:text-sm">
                <i class="fas fa-microphone mr-1"></i><span class="hidden sm:inline">Mute</span>
              </button>
              <button id="toggleCamera" class="bg-gradient-to-r from-gray-700 to-gray-900 hover:from-gray-800 hover:to-black text-white py-2 md:py-3 rounded-lg md:rounded-xl font-medium transition-all duration-300 transform hover:scale-105 shadow-lg text-xs md:text-sm">
                <i class="fas fa-video mr-1"></i><span class="hidden sm:inline">Camera</span>
              </button>
            </div>
            <p class="text-xs text-center text-gray-300 bg-black/20 py-2 px-3 rounded-xl border border-gray-600/20">
              <i class="fas fa-info-circle mr-1 text-yellow-400"></i>Allow camera/mic access when prompted
            </p>
          </div>

          <!-- Timer -->
          <div class="bg-black/30 backdrop-blur-lg rounded-2xl md:rounded-3xl p-4 md:p-6 border border-gray-600/30 shadow-2xl">
            <div class="text-center mb-4 md:mb-6">
              <h3 class="text-lg md:text-xl font-bold bg-gradient-to-r from-yellow-400 to-orange-400 bg-clip-text text-transparent">
                <i class="fas fa-clock mr-2 text-yellow-400"></i>Game Timer
              </h3>
            </div>
            <div class="space-y-3 md:space-y-4">
              <div class="bg-black/20 rounded-xl md:rounded-2xl p-3 md:p-4 border border-gray-600/20">
                <div class="text-xs text-gray-300 mb-2 text-center font-medium">White Player</div>
                <div id="white-timer" class="timer bg-gradient-to-r from-gray-900 to-black border border-gray-600/30">10:00</div>
              </div>
              <div class="bg-black/20 rounded-xl md:rounded-2xl p-3 md:p-4 border border-gray-600/20">
                <div class="text-xs text-gray-300 mb-2 text-center font-medium">Black Player</div>
                <div id="black-timer" class="timer bg-gradient-to-r from-gray-900 to-black border border-gray-600/30">10:00</div>
              </div>
            </div>
          </div>
        </div>

        <!-- Game Board Column -->
        <div class="lg:col-span-2 flex flex-col items-center justify-center">
          <div class="mb-4 md:mb-6 bg-black/30 backdrop-blur-lg rounded-xl md:rounded-2xl px-4 md:px-6 py-2 md:py-3 border border-gray-600/30 shadow-lg">
            <h2 class="text-lg md:text-2xl font-bold text-center bg-gradient-to-r from-yellow-400 via-orange-400 to-red-400 bg-clip-text text-transparent">
              <i class="fas fa-chess-board mr-2 text-yellow-400"></i>Chess Board
            </h2>
          </div>
          <div class="bg-black/30 backdrop-blur-lg rounded-2xl md:rounded-3xl p-2 md:p-3 border border-gray-600/30 shadow-2xl">
            <div id="board" class="chessboard shadow-2xl"></div>
          </div>
          <div class="mt-4 md:mt-6 text-center bg-black/30 backdrop-blur-lg rounded-xl md:rounded-2xl px-4 md:px-6 py-3 md:py-4 border border-gray-600/30 shadow-lg">
            <div id="gameStatus" class="text-base md:text-lg font-semibold bg-gradient-to-r from-green-400 to-blue-400 bg-clip-text text-transparent">
              <i class="fas fa-play mr-2 text-green-400"></i>Game in Progress
            </div>
            <div id="currentTurn" class="text-sm text-gray-300 mt-2 bg-black/20 px-3 py-1 rounded-full">
              White to move
            </div>
          </div>
        </div>

        <!-- Chat Column -->
        <div class="lg:col-span-1">
          <div class="bg-black/30 backdrop-blur-lg rounded-2xl md:rounded-3xl h-80 md:h-96 lg:h-full border border-gray-600/30 shadow-2xl overflow-hidden">
            <div class="h-full flex flex-col">
              <div class="p-4 md:p-6 border-b border-gray-600/30 bg-black/20">
                <h3 class="font-bold text-base md:text-lg bg-gradient-to-r from-yellow-400 to-orange-400 bg-clip-text text-transparent">
                  <i class="fas fa-comments mr-2 text-yellow-400"></i>Game Chat
                </h3>
              </div>
              
              <div id="chatMessages" class="flex-1 overflow-y-auto p-3 md:p-4 space-y-2 md:space-y-3 scrollbar-thin scrollbar-thumb-gray-600/50 scrollbar-track-transparent">
                <div class="text-center text-gray-300 text-sm py-4 md:py-6 bg-black/20 rounded-xl md:rounded-2xl border border-gray-600/20">
                  <i class="fas fa-info-circle mr-1 text-yellow-400"></i>
                  Chat with your opponent here!
                </div>
              </div>
              
              <div class="p-3 md:p-4 border-t border-gray-600/30 bg-black/20">
                <div class="flex space-x-2 md:space-x-3">
                  <input 
                    type="text" 
                    id="chatInput" 
                    placeholder="Type a message..." 
                    class="flex-1 bg-black/30 backdrop-blur-md border border-gray-600/30 rounded-lg md:rounded-xl px-3 md:px-4 py-2 md:py-3 text-sm text-white placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-yellow-400 focus:border-transparent transition-all"
                    maxlength="200"
                  >
                  <button 
                    id="sendMessage" 
                    class="bg-gradient-to-r from-gray-700 to-gray-900 hover:from-gray-800 hover:to-black px-3 md:px-4 py-2 md:py-3 rounded-lg md:rounded-xl transition-all duration-300 transform hover:scale-105 shadow-lg"
                  >
                    <i class="fas fa-paper-plane text-white"></i>
                  </button>
                </div>
                <div class="mt-2 md:mt-3 text-xs text-center text-gray-300 bg-black/20 py-1 px-3 rounded-lg">
                  Press Enter to send
                </div>
              </div>
            </div>
          </div>
        </div>

      </div>
    </main>

    <!-- Game Over Modal -->
    <div id="gameOverModal" class="fixed inset-0 bg-black/70 backdrop-blur-lg hidden items-center justify-center z-50 p-4">
      <div class="bg-black/30 backdrop-blur-xl rounded-2xl md:rounded-3xl p-6 md:p-10 max-w-md mx-4 text-center border border-gray-600/30 shadow-2xl">
        <div class="text-6xl md:text-8xl mb-4 md:mb-6">🏆</div>
        <h2 id="gameOverTitle" class="text-2xl md:text-3xl font-bold mb-3 md:mb-4 bg-gradient-to-r from-yellow-400 to-orange-400 bg-clip-text text-transparent">Game Over!</h2>
        <p id="gameOverMessage" class="text-gray-200 mb-6 md:mb-8 text-base md:text-lg">Checkmate! White wins!</p>
        <button id="newGameBtn" class="bg-gradient-to-r from-gray-700 to-gray-900 hover:from-gray-800 hover:to-black px-6 md:px-8 py-3 md:py-4 rounded-xl md:rounded-2xl font-bold text-base md:text-lg transition-all duration-300 transform hover:scale-105 shadow-lg">
          <i class="fas fa-redo mr-2"></i>New Game
        </button>
      </div>
    </div>

    <!-- Scripts -->
    <script src="https://cdn.socket.io/4.8.1/socket.io.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/chess.js/0.10.3/chess.min.js"></script>
    <script src="/js/chessgame.js"></script>
    <script src="/js/webrtc.js"></script>
    <script src="/js/chat.js"></script>
  </body>
</html>
